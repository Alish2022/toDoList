import React from 'react';

const StatusButtons = () => {
    return (
        <div>
            <button>All</button>
            <button>Active</button>
            <button>Completed</button>
        </div>
    );
};

export default StatusButtons;